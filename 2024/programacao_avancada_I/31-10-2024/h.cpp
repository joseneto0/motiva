#include <bits/stdc++.h>

using namespace std;

void solve(){
    int a, b;
    char s;
    cin >> a >> s >> b;
    cout << a + b << '\n';
}

int main(){
    ios_base::sync_with_stdio(0); cin.tie(NULL); cout.tie(NULL);
    int t;
    cin >> t;
    while (t--){
        solve();
    }
    return 0;
}